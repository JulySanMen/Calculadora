function suma(a, b) {
    // Definir variable desde un inicio
    return a + b;
}

let resultado = suma(5, 3); // Definir resultado
console.log(resultado); // Mostrar el resultado

function resta(a, b) {
    return a - b;
}
let resultadores = resta(9, 3);
console.log(resultadores);

function multi(a,b){
    return a*b;
}
let resultadomulti =multi(5,5);
console.log(resultadomulti);

